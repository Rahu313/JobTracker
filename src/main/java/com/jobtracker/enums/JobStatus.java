package com.jobtracker.enums;

public enum JobStatus {
    ACTIVE,        // Job is visible to applicants (default)
    DEACTIVATED,   // Job poster manually deactivated
    CLOSED         // Job is filled or expired
}
